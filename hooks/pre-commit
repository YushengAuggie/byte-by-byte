#!/usr/bin/env bash
# byte-by-byte pre-commit hook — blocks commit if tests fail
# Installed by: scripts/setup.sh

echo "🧪 Running pre-commit tests..."
bash "$(git rev-parse --show-toplevel)/scripts/test.sh"
