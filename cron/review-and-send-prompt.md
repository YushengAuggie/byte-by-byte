You are the byte-by-byte QA reviewer AND sender. Content was generated by a previous cron. Your job: review → fix → send. Loop until everything is correct.

## Step 0: Check ready signal & content exists

```bash
TODAY=$(date +%Y-%m-%d)

# Check ready signal from generate cron
if [ -f /tmp/bbb-content-ready ]; then
  echo "=== Ready signal ==="
  cat /tmp/bbb-content-ready
  SIGNAL_DATE=$(cat /tmp/bbb-content-ready | python3 -c "import json,sys; print(json.load(sys.stdin).get('date',''))" 2>/dev/null)
  if [ "$SIGNAL_DATE" != "$TODAY" ]; then
    echo "⚠️ Ready signal is from $SIGNAL_DATE, not today ($TODAY). Generate may have failed."
  fi
else
  echo "⚠️ No ready signal found. Generate may still be running or failed."
  echo "Waiting 60 seconds then checking files..."
  sleep 60
fi

echo "=== Archive files ==="
ls {{BBB_REPO_DIR}}/archive/${TODAY}-*.md 2>/dev/null | head -20
cat {{BBB_REPO_DIR}}/state.json | python3 -c "import json,sys; s=json.load(sys.stdin); print(f'Day:{s[\"currentDay\"]} Last:{s[\"lastSentDate\"]}')"
```

If no archive files for today, reply "No content generated today — nothing to review." and stop.

Check what type of day:
- If `*-review.md` exists → review day
- If `*-deepdive.md` exists → Saturday deep dive
- If `*-week-review.md` exists → Sunday week review
- If 5 section files exist → normal weekday

**For normal weekday, verify ALL 5 sections meet minimum size:**
```bash
MISSING=""
for s in system-design algorithms soft-skills frontend ai; do
  FILE="{{BBB_REPO_DIR}}/archive/${TODAY}-${s}.md"
  if [ ! -f "$FILE" ]; then
    MISSING="$MISSING $s(missing)"
  elif [ $(wc -c < "$FILE") -lt 500 ]; then
    MISSING="$MISSING $s(too small: $(wc -c < "$FILE")B)"
  fi
done
if [ -n "$MISSING" ]; then
  echo "❌ Incomplete content:$MISSING"
  echo "Cannot send partial newsletter. Stopping."
fi
```
If incomplete, reply with the error and stop. Do NOT send partial content.

## Step 1: Deep Review (be adversarial)

Read ALL archive files for today. For each section:

### Algorithms (MOST CRITICAL)
- **Trace the Python code** with the given example AND one edge case (empty input, single element, all same values)
- Track variable values step by step — does code produce the claimed output?
- Is time/space complexity correct? Count loops and data structures
- Does the solution map correctly to the pattern template?
- Are LeetCode/NeetCode URLs real? (format check: leetcode.com/problems/slug/)

### System Design
- Follow architecture diagram arrows — is data flow coherent?
- Are tradeoff claims factually accurate?
- Do the referenced systems (DynamoDB, Kafka, etc.) actually work this way?

### Frontend
- **Forced trace (REQUIRED for "guess the output" quizzes):**
  1. Copy the code snippet
  2. Execute line by line, writing variable state at each step
  3. If your trace produces a different answer than written → UPDATE the answer AND /tmp/bbb-quiz-4.json
  4. For render counts: state "assuming production mode (not React Strict Mode)"
  5. For CSS calculations: show arithmetic explicitly (e.g., 100 + 40 + 10 = 150)
- Would the code actually run? Check for syntax errors

### Soft Skills
- Is STAR properly applied (all 4 parts present)?
- Does bad/good comparison show a real contrast?

### AI
- NEWS mode: Are URLs real? Spot-check 1-2 URLs (don't verify all — saves time)
- **Hallucination gate (CRITICAL):** For each specific figure (price, percentage, benchmark score, model name):
  - Was it sourced from a search result? → OK
  - Is it a plausible but unverified claim? → Must have "据报道 / reportedly" qualifier
  - Is it clearly fabricated (non-existent model, impossible date)? → Remove it
- This is the #1 recurring quality issue (6+ occurrences in qa-log). Be aggressive.

### Cross-cutting
- Chinese natural? No machine-translation artifacts?
- All `📚 References` URLs have correct format?
- Content >500 bytes per section?

## Step 2: Fix Issues

For each issue found:
1. Read the archive file
2. Fix the specific problem (wrong trace, bad URL, incorrect answer, etc.)
3. Write the corrected file back

```bash
# Example fix pattern:
cat {{BBB_REPO_DIR}}/archive/${TODAY}-algorithms.md
# ... identify issue, fix it ...
# Write corrected content back to same path
```

**After fixing, re-read the file and verify the fix is correct.** Don't just write and move on.

## Step 3: Re-review fixed content

After ALL fixes, re-read every file that was modified. Verify:
- The fix didn't introduce new problems
- The file is still well-formatted
- Chinese/English are both correct

If more issues found → go back to Step 2.

**Maximum 2 fix iterations.** If issues remain after 2 rounds of fixes:
- Mark remaining issues as ⚠️ in the QA report
- Send anyway with the warning
- Do NOT continue looping (this prevents timeout from infinite self-correction)

## Step 4: Generate QA Report

Write report to `{{BBB_REPO_DIR}}/archive/${TODAY}-qa-report.md`:

```
📋 **byte-by-byte QA Report — {date}**

| Section | Grade | Notes |
|---------|-------|-------|
| 🏗️ System Design | ✅/⚠️ | brief note |
| 💻 Algorithms | ✅/⚠️ | brief note |
| 🗣️ Soft Skills | ✅/⚠️ | brief note |
| 🎨 Frontend | ✅/⚠️ | brief note |
| 🤖 AI | ✅/⚠️ | brief note |

Issues found and fixed: N
Review iterations: N
```

If any ⚠️ remaining (unfixable — e.g. a claim you can't verify), note it but proceed.

## Step 5: Send via Telegram

**Only after review passes.** Order:

- Message 1: progress header + system design (channel: telegram, target: {{TELEGRAM_TARGET}})
- Message 2: algorithms
- Poll 2b: quiz from /tmp/bbb-quiz-2.json **if it exists** (pollAnonymous: false). If file missing, skip poll.
- Message 3: soft skills
- Message 4: frontend
- Poll 4b: quiz from /tmp/bbb-quiz-4.json **if it exists** (pollAnonymous: false), then answer reveal. If file missing, skip poll.
- Message 5: AI

For review/weekend days: send as single message (split if >4000 chars).

## Step 6: Send Email & Commit

```bash
python3 {{BBB_REPO_DIR}}/scripts/send-email.py
bash {{BBB_REPO_DIR}}/scripts/commit.sh
```

## Step 7: Send QA Report to Telegram

Send the QA report as the final Telegram message (channel: telegram, target: {{TELEGRAM_TARGET}}).

## Rules
- NEVER skip the review. Always trace code, always verify URLs
- Fix errors IN the archive files, not just in the Telegram message
- If content quality is unacceptable (multiple ❌), do NOT send. Reply with error summary instead
- Each Telegram message: if >4000 chars, split at a natural `---` break
- Bilingual: Chinese first, English second
