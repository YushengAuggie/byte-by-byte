# Machine-specific config (contains personal info)
config.env

# Temp files from generation
/tmp/bbb-section-*.txt

# OS files
.DS_Store

# Delivery logs (local only — keep the directory but not its contents)
logs/*.log
