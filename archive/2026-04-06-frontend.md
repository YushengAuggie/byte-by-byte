🎨 **前端 Day 16 / Frontend Day 16**

### 💡 场景 / Scenario
你在做一个 Dashboard，里面有图表组件、用户信息组件和列表组件。如果图表组件因为后端返回了脏数据而抛出 JavaScript 错误，整个页面都会变成白屏！你需要一种方法，让报错的组件只显示一个“加载失败”的提示，而不影响页面上其他正常的组件。同时，对于加载慢的组件，你想优雅地显示一个 Loading 骨架屏。
*You're building a dashboard with charts, user info, and lists. If the chart component throws a JS error due to bad backend data, the entire page goes blank! You need a way to catch the error locally so only the chart shows an "Error" state, keeping the rest of the app alive. Also, for slow components, you want to elegantly show a loading skeleton.*

### 💻 核心概念 / Core Concept: Error Boundaries & Suspense

React 提供了两个强大的边界组件来处理异步加载和错误状态：
1. **Error Boundary (错误边界)**: 捕获子组件树中的 JS 错误，渲染备用 UI。
2. **Suspense (悬念)**: 在子组件准备好（例如加载代码分割的 chunk 或获取数据）之前，渲染后备 UI（如 Loading Spinner）。

```tsx
import { Suspense } from 'react';
import { ErrorBoundary } from 'react-error-boundary'; // 推荐使用这个库

// 懒加载重型组件
const HeavyChart = React.lazy(() => import('./HeavyChart'));

function Dashboard() {
  return (
    <div className="dashboard">
      <Sidebar />
      <main>
        {/* 错误边界：如果 Chart 崩溃，只在这里显示错误，不影响 Sidebar */}
        <ErrorBoundary fallback={<div className="error">图表加载失败</div>}>
          {/* Suspense：在 Chart 代码下载完成前显示 Loading */}
          <Suspense fallback={<div className="skeleton-loader">Loading Chart...</div>}>
            <HeavyChart />
          </Suspense>
        </ErrorBoundary>
      </main>
    </div>
  );
}
```

### 🧠 猜猜输出什么？ / Quiz
如果 `HeavyChart` 内部的 `useEffect` 里面有一个 `setTimeout`，在 2 秒后抛出了一个错误，`ErrorBoundary` 能捕获到吗？
A) 能
B) 不能

*(答案在底部 / Answer at the bottom)*

### ❌ 常见误区 vs ✅ 正确做法
- ❌ **Bad**: 在每个组件内部写大量的 `try/catch` 和 `if (loading)` 逻辑，导致 UI 逻辑和数据逻辑严重耦合。
- ✅ **Good**: 使用声明式的 `<Suspense>` 和 `<ErrorBoundary>` 将加载状态和错误处理提升到父级，让业务组件只关注快乐路径（Happy Path）。

### ⚖️ 何时使用 / When to use
- **使用 Error Boundaries**: 在应用的顶层（捕获致命错误），以及在主要的布局区块（如侧边栏、主内容区）周围，防止局部崩溃导致全局白屏。
- **使用 Suspense**: 配合 `React.lazy` 进行代码分割（Code Splitting），或者在使用 Next.js 13+ App Router 时处理服务器组件的数据获取。

### 📚 延伸阅读 / References
- [React Docs: Catching rendering errors with an error boundary](https://react.dev/reference/react/Component#catching-rendering-errors-with-an-error-boundary)
- [React Docs: Suspense](https://react.dev/reference/react/Suspense)
- [npm: react-error-boundary](https://www.npmjs.com/package/react-error-boundary)

### 🧒 ELI5 (Explain Like I'm 5)
Suspense 就像餐厅的叫号器。你点了一份需要做很久的大餐（重型组件），服务员先给你一个叫号器（Loading 状态），等菜做好了再给你端上来。
Error Boundary 就像船上的水密舱。如果船底破了一个洞（组件报错），水密舱门会关上，把水限制在一个小房间里，这样整艘船（整个网页）就不会沉没。

---
*Quiz Answer: B) 不能。Error Boundary 只能捕获**渲染期间**、生命周期方法和子组件构造函数中的错误。它**不能**捕获事件处理器（Event Handlers）、异步代码（如 `setTimeout` 或 `fetch` 回调）中的错误。对于异步错误，你需要手动 catch 并设置状态，或者抛出给 Error Boundary。*