🤖 **AI Day 8 (2 min read) — AI 概念 / AI Concept**
**Training vs Fine-Tuning vs Prompting — 训练 vs 微调 vs 提示**

---

**直觉理解 / Intuitive Explanation**

想象你在培养一个厨师：

🏫 **Training（训练）** — 从零开始上烹饪学校。学所有菜系、刀工、火候。耗时数年，花费巨大，但出来是个全能厨师。
→ GPT-4 的预训练：万亿 token，数千 GPU，数百万美元。

🎯 **Fine-Tuning（微调）** — 全能厨师去日本学了 3 个月寿司。基础能力不变，但寿司水平大幅提升。
→ 在特定数据集上继续训练：医疗、法律、代码等。成本中等，效果显著。

💬 **Prompting（提示）** — 直接告诉厨师"今天做川菜，要麻辣，少油"。不改变厨师本身的能力，只是给出指令。
→ System prompt、few-shot examples。零成本，即时生效，但受限于模型已有能力。

---

**对比表 / Comparison**

```
                Training      Fine-Tuning     Prompting
─────────────────────────────────────────────────────
数据量          TB级           千~万条          0~几十条
成本            $$$$$          $$              $0
时间            周~月          小时~天          秒
需要 GPU?       是 (大量)      是 (少量)        否
改变模型?       创建模型       修改权重         不改变
适用场景        通用基座       领域专精         日常使用
```

---

**实际应用 / Real-World Examples**

| 方法 | 例子 |
|------|------|
| Training | Meta 训练 LLaMA 3 — 15T tokens, 数千 H100 |
| Fine-Tuning | 医院用 GPT-4 微调 → 专业医疗问答模型 |
| Prompting | "你是一个资深 Python 开发者，请 review 这段代码" |

**现实中的组合拳：** OpenAI 的 ChatGPT = 预训练 (Training) + RLHF 微调 (Fine-Tuning) + System Prompt (Prompting)。三者不是互斥的！

---

**可运行代码 / Runnable Code**

```python
# pip install openai && python script.py
# 展示 Prompting 如何改变同一模型的行为
from openai import OpenAI
client = OpenAI()  # uses OPENAI_API_KEY env var

prompts = [
    ("You are a pirate.", "What is Python?"),
    ("You are a professor.", "What is Python?"),
]
for system, user in prompts:
    r = client.chat.completions.create(
        model="gpt-4o-mini",
        messages=[{"role":"system","content":system},
                  {"role":"user","content":user}],
        max_tokens=60
    )
    print(f"[{system}]\n{r.choices[0].message.content}\n")
```

---

**什么时候用什么？/ Decision Tree**

```
需要全新能力？ → Training（但你可能不需要）
     ↓ 不需要
现有模型 + 提示不够好？ → Fine-Tuning
     ↓ 够好
直接 Prompting！ → 90% 的场景用这个就够了
```

**黄金法则：先试 Prompting，不行再 Fine-Tune，极少数情况才需要 Training。**

---

📚 **深入学习 / Learn More:**
• [OpenAI Fine-Tuning Guide](https://platform.openai.com/docs/guides/fine-tuning)
• [Hugging Face: Training & Fine-Tuning](https://huggingface.co/docs/transformers/training)
• [Prompt Engineering Guide](https://www.promptingguide.ai/)

🧒 **ELI5:** Training is like going to school for years to learn everything; fine-tuning is like taking a special class to get really good at one thing; and prompting is like asking your smart friend a question — they already know stuff, you just tell them what you need!
