🤖 **AI Day 18**

### 🧠 核心概念 / Core Concept: Vector Databases (向量数据库)

传统数据库（如 MySQL、PostgreSQL）擅长精确匹配（Exact Match）："找到名字叫 'Alice' 的用户"。
但在 AI 时代，我们需要**语义搜索（Semantic Search）**："找到和 '一只可爱的橘猫' 意思相近的图片或文本"。
这就是**向量数据库（Vector Databases）**的用武之地。它们专门用于存储和快速检索高维向量（Embeddings）。

*Traditional databases excel at exact matches. In the AI era, we need semantic search. Vector databases are purpose-built to store and quickly retrieve high-dimensional vectors (embeddings).*

### ⚙️ 它是如何工作的？ / How it works
1. **Embedding**: 将文本、图像或音频通过 AI 模型（如 OpenAI 的 `text-embedding-ada-002`）转换为一串数字（向量）。
2. **Storage**: 向量数据库存储这些数字串。
3. **Query**: 当你搜索时，将你的搜索词也变成向量。
4. **Similarity Search**: 数据库计算搜索向量与存储向量之间的“距离”（如余弦相似度 Cosine Similarity）。距离越近，语义越相似。

为了在数百万个向量中快速找到最相似的，向量数据库使用了近似最近邻（ANN, Approximate Nearest Neighbor）算法，如 HNSW (Hierarchical Navigable Small World)。

### 🛠️ 常见工具 / Common Tools
- **Pinecone**: 完全托管的云原生向量数据库，闭源，极其易用。
- **Weaviate / Milvus / Qdrant**: 开源的向量数据库，支持自托管。
- **pgvector**: PostgreSQL 的一个扩展插件。如果你已经在使用 Postgres，加上这个插件就能直接获得向量搜索能力，无需引入新的基础设施。

### 💻 Python 实战 / Runnable Python Snippet
这里使用内存中的轻量级向量库 `ChromaDB` 来演示：

```python
# pip install chromadb
import chromadb

# 1. Initialize local memory client
client = chromadb.Client()
collection = client.create_collection("my_documents")

# 2. Add documents (Chroma automatically handles embedding under the hood)
collection.add(
    documents=["The cat is sleeping on the sofa", "Dogs love to play fetch in the park", "I like to eat pizza"],
    metadatas=[{"source": "cat_book"}, {"source": "dog_book"}, {"source": "food_blog"}],
    ids=["id1", "id2", "id3"]
)

# 3. Query by semantics
results = collection.query(
    query_texts=["Tell me about pets"],
    n_results=2
)

print(results["documents"]) 
# Output will likely prioritize the cat and dog sentences over the pizza one!
```

### 📚 延伸阅读 / References
- [Pinecone: What is a Vector Database?](https://www.pinecone.io/learn/vector-database/)
- [pgvector GitHub Repository](https://github.com/pgvector/pgvector)
- [ChromaDB Documentation](https://docs.trychroma.com/)

### 🧒 ELI5 (Explain Like I'm 5)
想象一个巨大的图书馆，传统数据库是按字母顺序排列书名的目录柜。如果你拼错了一个字母，就找不到书。
向量数据库就像一个超级聪明的图书管理员。你对他说：“我想找一本关于太空旅行和外星人的小说，风格有点像星球大战。” 他不需要精确的书名，就能直接把你带到最符合你描述的书架前。