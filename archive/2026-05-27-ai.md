# 🤖 AI — Evaluation: How to Measure AI Quality
*Day 52 · Expert Phase · CONCEPT Mode · ~2 min read*

---

## 核心问题 / The Core Problem

你怎么知道一个 AI 模型"好不好"？准确率 95%？听起来很棒 — 但如果测试集被污染了呢？如果模型"背题"了呢？如果你的任务在测试集上根本没有出现呢？

**AI 评估是 AI 开发中最难的问题之一。**

*How do you know if an AI model is "good"? 95% accuracy sounds great — but what if the test set was contaminated? What if the model memorized answers? What if your real task never appeared in the benchmark?*

---

## 三层评估框架 / Three Evaluation Layers

```
Layer 3: Production Monitoring (最终真相)
─────────────────────────────────────────
  Real user traffic → live feedback → drift detection
  "Did users actually like the output?"

Layer 2: Offline Evaluation (发布前)
─────────────────────────────────────
  ┌─────────────────┬─────────────────┬─────────────────┐
  │  Automatic      │  LLM-as-Judge   │  Human Eval     │
  │  Metrics        │                 │                 │
  │  (BLEU, ROUGE,  │  GPT-4 rates    │  Human raters   │
  │  Exact Match,   │  outputs, 80-   │  Gold standard  │
  │  F1, Perplexity)│  90% agree w/   │  but expensive  │
  │                 │  humans         │                 │
  └─────────────────┴─────────────────┴─────────────────┘

Layer 1: Benchmarks (能力基线)
────────────────────────────────
  MMLU, HumanEval, GPQA, SWE-bench...
  Public, reproducible, but can be gamed
```

---

## 关键评估指标 / Key Metrics

### 分类任务
```python
from sklearn.metrics import classification_report
# Precision, Recall, F1 per class
# Use when: spam detection, sentiment, intent classification
```

### 生成任务 (RAG / QA)
```python
# pip install ragas
from ragas import evaluate
from ragas.metrics import faithfulness, answer_relevancy

# faithfulness: does answer match retrieved context?
# answer_relevancy: does answer address the question?
results = evaluate(dataset, metrics=[faithfulness, answer_relevancy])
```

### LLM-as-Judge 模式
```python
# pip install openai
from openai import OpenAI

def llm_judge(question: str, answer: str, reference: str) -> dict:
    client = OpenAI()
    prompt = f"""
    Rate this answer on a scale of 1-5 for:
    1. Correctness (matches reference)
    2. Completeness (covers all key points)
    3. Clarity (easy to understand)
    
    Question: {question}
    Answer: {answer}
    Reference: {reference}
    
    Return JSON: {{"correctness": N, "completeness": N, "clarity": N, "reasoning": "..."}}
    """
    response = client.chat.completions.create(
        model="gpt-4o",
        messages=[{"role": "user", "content": prompt}],
        response_format={"type": "json_object"}
    )
    return response.choices[0].message.content
```

---

## 基准污染问题 / Benchmark Contamination

传统 MMLU 已被大量模型"背熟" — 模型的训练数据包含了测试集。这导致：

```
MMLU 2024: GPT-4 = 86% ← "amazing!"
MMLU-Pro 2025: GPT-4 = 72% ← harder, less contaminated
Humanity's Last Exam (2025): Best models ~45% ← expert-level, hard to game
```

**解决方案：**
- 使用**动态基准** — 每次评测随机生成题目
- 使用**私有测试集** — 不公开，防止训练数据泄漏
- **SWE-bench** — 用真实 GitHub issues 测代码能力，难以"背"

---

## 生产环境监控 / Production Monitoring

```python
# Real evaluation: track what actually matters in production
metrics_to_monitor = {
    "user_thumbs_up_rate": "> 80%",      # direct user feedback
    "hallucination_rate": "< 5%",         # factual errors
    "latency_p99": "< 2000ms",            # user experience
    "refusal_rate": "1-5%",               # too high = over-cautious
    "context_relevance": "> 0.85",        # RAG quality
}
# Tools: Langfuse, Arize AI, W&B Weave, Humanloop
```

---

## 📚 参考资料 / References

- [RAGAS — RAG Evaluation Framework](https://docs.ragas.io/) — open source, widely used
- [SWE-bench — Real-world code evaluation](https://www.swebench.com/) — GitHub issues benchmark
- [Kili Technology — AI Benchmarks Guide 2026](https://kili-technology.com/blog/ai-benchmarks-guide-the-top-evaluations-in-2026-and-why-theyre-not-enough)

---

## 🧒 ELI5

衡量 AI 好不好，就像评分一个学生的期末考试。问题是：如果这个学生提前看到了考题（数据污染），成绩就不可信了。所以现在大家开始用"每次临时出题"（动态基准），或者直接看这个学生在真实工作中表现如何（生产环境监控）。

*Measuring AI quality is like grading a student's final exam. The problem: if the student saw the questions ahead of time (data contamination), their score is meaningless. So now we use "dynamically generated questions" (dynamic benchmarks) or just watch how the student performs in their real job (production monitoring).*
