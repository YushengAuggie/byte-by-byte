🎨 **前端 Day 8 (2 min read) / Frontend Day 8**
**CSS Animations & Transitions — CSS 动画与过渡**

---

**猜猜这段代码输出什么？/ What does this code output?**

```css
.box {
  width: 100px;
  height: 100px;
  background: coral;
  transition: transform 0.3s ease;
}
.box:hover {
  transform: scale(1.2) rotate(10deg);
}
```

**What happens when you hover over the box?**
A) Box instantly becomes 120% larger and rotates 10°
B) Box smoothly grows to 120% and rotates 10° over 0.3s
C) Box only scales (rotate is ignored because of transition property)
D) Nothing — transition doesn't work with transform

*(答案在下方投票后揭晓！)*

---

**🗺️ Transition vs Animation 速查**

```
transition:                     animation:
  触发条件: 状态变化 (hover等)     触发条件: 自动播放/手动触发
  控制: 起点 → 终点               控制: 多个关键帧 @keyframes
  次数: 一次 (每次触发)            次数: 可无限循环
  适用: 简单交互效果               适用: 复杂/持续动画
```

---

**代码示例**

```css
/* Transition — 简单悬停效果 */
.button {
  background: #7c5cfc;
  transition: background 0.2s ease, transform 0.2s ease;
}
.button:hover {
  background: #6344e0;
  transform: translateY(-2px); /* 微微上浮 */
}

/* Animation — 持续脉冲效果 */
@keyframes pulse {
  0%, 100% { transform: scale(1); }
  50%      { transform: scale(1.05); }
}
.notification-badge {
  animation: pulse 2s ease-in-out infinite;
}

/* Animation — 淡入 */
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(20px); }
  to   { opacity: 1; transform: translateY(0); }
}
.card {
  animation: fadeIn 0.5s ease-out forwards;
}
```

---

**你可能不知道 / Common Gotchas**

⚠️ **`transition: all` 的性能陷阱**
`transition: all 0.3s` 看起来方便，但会监听所有属性变化 — 包括 `width`, `height` 等触发 layout 的属性。只 transition 你需要的属性！

⚠️ **`display: none` 无法过渡**
从 `display: none` 到 `display: block` 没有过渡效果。替代方案：用 `opacity` + `visibility` + `pointer-events`。

⚠️ **GPU 加速属性**
`transform` 和 `opacity` 由 GPU 合成，性能极好。`width`, `height`, `top`, `left` 触发 layout reflow，性能差。**永远优先用 transform！**

---

**🎯 Mini Challenge**

写一个加载动画：三个圆点依次上下跳动（staggered bounce）：
```
  ●     ●     ●
  ↕     ↕     ↕
```
提示：同一个 `@keyframes`，三个元素用 `animation-delay` 错开时间。

---

📚 **深入学习 / Learn More:**
• [MDN: Using CSS transitions](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_transitions/Using_CSS_transitions)
• [web.dev: Animations Guide](https://web.dev/articles/animations-guide)
• [CSS-Tricks: A Complete Guide to CSS Animations](https://css-tricks.com/almanac/properties/a/animation/)

🧒 **ELI5:** CSS transitions are like slowly turning a dimmer switch — things change smoothly instead of suddenly. CSS animations are like a flip-book — you draw each frame, and the browser plays them in order!
