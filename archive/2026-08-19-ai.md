# AI 综合 — RAG + 向量数据库 + 结构化输出的完整生产流水线
# AI Synthesis — Full Production Pipeline: RAG + Vector DB + Structured Output

> Day 114 · Synthesis Mode · Expert Phase

---

## 🤖 AI

**把学过的 AI 概念串成一条生产级流水线**
**Connecting Learned AI Concepts into a Production-Grade Pipeline**

---

### 🎯 场景 / Scenario

你要构建一个企业内部知识库问答系统：上传文档 → 自动索引 → 用户提问 → AI 返回结构化答案（含来源引用）。

Build an internal enterprise Q&A system: upload docs → auto-index → user asks → AI returns structured answer with citations.

---

### 🏗️ 完整架构 / Full Architecture

```
概念地图 / Concept Map (Synthesis of Covered Topics)

  Document Upload
       │
       ▼
  [Chunking] ──── context windows (Day 34)
       │
       ▼
  [Embeddings] ── embeddings (Day 6), tokenization (Day 4)
       │
       ▼
  [Vector DB] ─── vector databases (Day 18)
  (pgvector/Weaviate/Pinecone)
       │
  ┌────┴─────────────────────────┐
  │         Query Time           │
  └────┬─────────────────────────┘
       │ User Question
       ▼
  [Embed Query] ── same embedding model
       │
       ▼
  [ANN Search] ─── top-k similar chunks
       │
       ▼
  [Prompt Assembly] ── RAG (Day 16), prompt engineering (Day 22)
       │
       ▼
  [LLM Call] ──────── function calling (Day 32)
       │
       ▼
  [Structured Output] ── structured output (Day 64)
       │
       ▼
  {"answer": "...", "citations": [...], "confidence": 0.92}
```

---

### 💻 可运行代码 / Runnable Pipeline

```python
# pip install openai chromadb tiktoken
import json
import chromadb
from openai import OpenAI

client = OpenAI()
chroma = chromadb.Client()
collection = chroma.create_collection("knowledge_base")

# Step 1: Index documents (Embeddings + Vector DB)
def index_document(doc_id: str, text: str):
    # Chunk by ~500 tokens (context window awareness)
    chunks = [text[i:i+2000] for i in range(0, len(text), 2000)]
    
    for idx, chunk in enumerate(chunks):
        response = client.embeddings.create(
            input=chunk,
            model="text-embedding-3-small"
        )
        embedding = response.data[0].embedding
        
        collection.add(
            documents=[chunk],
            embeddings=[embedding],
            ids=[f"{doc_id}_chunk_{idx}"],
            metadatas=[{"doc_id": doc_id, "chunk": idx}]
        )

# Step 2: Query with Structured Output
def answer_question(question: str) -> dict:
    # Embed the question
    q_embed = client.embeddings.create(
        input=question,
        model="text-embedding-3-small"
    ).data[0].embedding
    
    # ANN search (Vector DB)
    results = collection.query(
        query_embeddings=[q_embed],
        n_results=3
    )
    context = "\n\n".join(results["documents"][0])
    
    # RAG: assemble prompt
    prompt = f"""Answer based ONLY on the context below.
Context:
{context}

Question: {question}

Return JSON: {{"answer": str, "citations": [str], "confidence": float 0-1}}"""
    
    # Structured output (JSON mode)
    response = client.chat.completions.create(
        model="gpt-4o-mini",
        messages=[{"role": "user", "content": prompt}],
        response_format={"type": "json_object"}  # Structured output
    )
    
    return json.loads(response.choices[0].message.content)

# Usage
index_document("policy_v2", "Our vacation policy allows 20 days per year...")
result = answer_question("How many vacation days do I get?")
# {"answer": "20 days per year", "citations": ["policy_v2"], "confidence": 0.95}
```

---

### 🔗 综合联系 / Concepts Synthesized

| Day | Concept | 在这个流水线中的角色 |
|-----|---------|---------------------|
| 4 | Tokenization | 决定 chunk 大小 |
| 6 | Embeddings | 文档和查询的向量化 |
| 16 | RAG | 整体架构模式 |
| 18 | Vector DB | ChromaDB/pgvector 选型 |
| 22 | Prompt Engineering | context 注入格式 |
| 32 | Function Calling | 可替换为 tool-based retrieval |
| 34 | Context Windows | chunk 大小决策 |
| 64 | Structured Output | JSON mode 保证输出可解析 |

---

### ⚠️ 生产级陷阱 / Production Gotchas

1. **Embedding model 一致性** — 索引和查询必须用同一个 embedding model，换了模型要全量重新索引
2. **Chunk overlap** — 纯切割会断上下文，用 overlap=200 tokens 的滑动窗口
3. **Hallucination with RAG** — 模型可能"脑补"不在 context 里的内容，加 `Answer ONLY based on context` 约束
4. **Embedding 成本** — 百万文档 × embedding API = $$，考虑批量处理 + 本地模型 (sentence-transformers)

---

### 📚 References
- https://python.langchain.com/docs/tutorials/rag/
- https://www.trychroma.com/
- https://platform.openai.com/docs/guides/embeddings
- https://platform.openai.com/docs/guides/structured-outputs

### 🧒 ELI5
把文档变成数字向量存起来（就像给每段话拍了一张"语义指纹"）。用户提问时，找出最相似的几段话，让 AI 读这几段话来回答——而不是靠记忆瞎猜。
Turn documents into number vectors (like semantic fingerprints). When a user asks, find the most similar passages and have the AI answer from those — not from memory or guesswork.
