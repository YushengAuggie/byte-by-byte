# 🎨 前端 / Frontend — Day 19
**Topic:** React Server Components (RSC) — What Actually Changes
**Date:** 2026-04-07

---

🎨 **前端 / Frontend**

**React Server Components (RSC) — 到底改变了什么？**
**React Server Components (RSC) — What Actually Changes?**

---

### 🌍 真实场景 / Real-World Scenario

你在做一个博客文章页面。文章内容很长，包含大量 Markdown 解析库（比如 `marked`，体积很大）。如果用传统的 React（客户端渲染），用户需要把整个 `marked` 库下载到浏览器，然后才能看到文章。这太慢了！

*You're building a blog post page. The content is long and uses a massive Markdown parsing library (e.g., `marked`). With traditional React (Client-Side Rendering), the user has to download the entire `marked` library to their browser just to see the post. That's way too slow!*

---

### 💻 代码对比 / Code Snippet

**❌ 传统 React (Client Component):**
```tsx
import { marked } from 'marked'; // 🚨 整个库被打包发给浏览器！(Bundle size ++!)

export default function BlogPost({ content }) {
  // 浏览器在运行时解析 Markdown
  const html = marked.parse(content);
  return <div dangerouslySetInnerHTML={{ __html: html }} />;
}
```

**✅ React Server Component (RSC - Next.js App Router 默认):**
```tsx
import { marked } from 'marked'; // 🟢 只在服务器端运行，不发给浏览器！(Zero bundle size!)

// 注意：这是一个 async 组件！(RSC 独有超能力)
export default async function BlogPost({ id }) {
  // 直接在组件里查数据库！(Mind blown 🤯)
  const post = await db.posts.findUnique({ where: { id } });
  
  // 服务器解析完 Markdown，直接把生成的 HTML 发给浏览器
  const html = marked.parse(post.content);
  return <div dangerouslySetInnerHTML={{ __html: html }} />;
}
```

---

### 🧠 核心概念 / Core Concept

RSC 的本质是：**把组件分成两类，各司其职。**

1. **Server Components (默认):**
   - 跑在服务器上。
   - 负责：获取数据（直接读 DB）、繁重的计算（解析 Markdown）。
   - 结果：它们的代码**永远不会**发送到浏览器。浏览器只收到它们渲染出的最终 HTML/UI 树。

2. **Client Components (`"use client"`):**
   - 跑在浏览器上（但也支持 SSR 预渲染）。
   - 负责：交互（`onClick`）、状态（`useState`）、生命周期（`useEffect`）、浏览器 API（`window`）。
   - 结果：它们的代码会被打包发送给浏览器。

*RSC splits components into two types:*
1. ***Server Components (Default):** Run on the server. Fetch data directly, do heavy lifting. Their code is **never** sent to the client. The client only gets the resulting UI.*
2. ***Client Components (`"use client"`):** Run on the client. Handle interactivity, state, and browser APIs. Their code is bundled and sent to the client.*

---

### ⚠️ 常见误区 / Common Mistake

❌ **误区：** "Server Components 就是 SSR (Server-Side Rendering)。"
✅ **真相：** 不是！SSR 是把整个页面在服务器上渲染成 HTML，然后发给浏览器，浏览器还要下载所有的 JS 代码去 "hydrate"（注水，绑定事件）。RSC 是**连 JS 代码都不发了**，直接发渲染好的 UI 树。

❌ **误区：** "所有的组件都应该变成 Server Components。"
✅ **真相：** 需要交互的地方（按钮、输入框、动画）必须用 Client Components。RSC 和 Client Components 是互补的。

---

### 🎯 什么时候用什么？ / When to use what?

| 场景 | Server Component | Client Component (`"use client"`) |
|------|------------------|-----------------------------------|
| 获取数据 (Fetch data) | ✅ | ❌ (尽量避免) |
| 直接访问后端资源 (DB, API keys) | ✅ | ❌ (安全风险) |
| 保持大依赖包在服务器 (Large dependencies) | ✅ | ❌ |
| 添加交互 (onClick, onChange) | ❌ | ✅ |
| 使用状态和生命周期 (useState, useEffect) | ❌ | ✅ |
| 使用浏览器 API (window, localStorage) | ❌ | ✅ |

---

### 🧩 猜猜这段代码输出什么？ / Quiz

```tsx
// app/page.tsx (Next.js App Router)
import Button from './Button';

export default function Page() {
  console.log("Where am I running?");
  return <Button />;
}
```

**选项：**
A. 浏览器的控制台
B. 服务器的终端 (Terminal)
C. 都会打印
D. 报错

*(答案见下方 JSON)*

---

### 📚 参考资料 / References
1. [React Server Components — React Docs](https://react.dev/reference/rsc/server-components)
2. [Next.js App Router — Server Components](https://nextjs.org/docs/app/building-your-application/rendering/server-components)
3. [Making Sense of React Server Components — Josh W. Comeau](https://www.joshwcomeau.com/react/server-components/)

---

### 🧒 ELI5

以前去餐厅（传统 React），厨师（服务器）把生的菜和菜谱（JS 代码）打包寄到你家，让你自己炒（浏览器渲染）。现在有了 RSC，厨师在后厨把菜炒好（服务器渲染），直接端上桌（HTML），你只管吃就行了！只有需要你自己加盐加醋的菜（交互），才会给你调料瓶（Client Components）。

*Before (Traditional React), the chef (server) mailed you raw ingredients and a recipe (JS bundle) to cook at home (browser rendering). Now with RSC, the chef cooks the heavy meals in the kitchen and serves them ready-to-eat (HTML). You only get the salt shaker (Client Components) for dishes you need to interact with!*