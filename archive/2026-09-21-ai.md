# 🤖 AI 综合深潜 / AI Synthesis Deep Dive — Day 134

**从 Embeddings 到 RAG 到 Agent：向量技术全栈复盘**
**From Embeddings to RAG to Agents: Full-Stack Vector Technology Review**

> 我们已经覆盖了 Embeddings、RAG、Vector Databases、AI Agents 等独立主题。今天把它们串联成一个完整的生产系统。
> We've covered Embeddings, RAG, Vector DBs, and Agents separately. Today: wire them into one production system.

---

## 完整架构 / Full Architecture

```
[User Query]
     ↓
[Embedding Model]  → query_vector (e.g., text-embedding-3-small)
     ↓
[Vector DB]        → top-k similar chunks (cosine similarity)
 (pgvector/Pinecone)
     ↓
[Reranker]         → cross-encoder reranking (optional but improves quality)
     ↓
[LLM Context]      → prompt = system + retrieved_chunks + query
     ↓
[LLM]              → answer
     ↓
[Guardrails]       → check for hallucination, PII, toxicity
     ↓
[Response]
```

---

## 可运行代码 / Runnable Code

```python
# pip install openai numpy scikit-learn
import numpy as np
from openai import OpenAI

client = OpenAI()  # uses OPENAI_API_KEY env var

# ── Step 1: Embed documents ──────────────────────────
def embed(text: str) -> list[float]:
    resp = client.embeddings.create(
        model="text-embedding-3-small",
        input=text
    )
    return resp.data[0].embedding

# Simulated document store
docs = [
    "Python's GIL prevents true thread parallelism for CPU work.",
    "Redis uses single-threaded event loop for atomicity.",
    "PostgreSQL uses MVCC for concurrent transaction isolation.",
]
doc_vectors = np.array([embed(d) for d in docs])

# ── Step 2: Retrieve (cosine similarity) ─────────────
def retrieve(query: str, top_k: int = 2) -> list[str]:
    q_vec = np.array(embed(query))
    # cosine similarity = dot product of normalized vectors
    norms = np.linalg.norm(doc_vectors, axis=1) * np.linalg.norm(q_vec)
    scores = doc_vectors @ q_vec / norms
    top_indices = np.argsort(scores)[::-1][:top_k]
    return [docs[i] for i in top_indices]

# ── Step 3: Generate with context ────────────────────
def rag_query(question: str) -> str:
    context_chunks = retrieve(question)
    context = "\n".join(f"- {c}" for c in context_chunks)
    
    resp = client.chat.completions.create(
        model="gpt-4o-mini",
        messages=[
            {"role": "system", "content": "Answer using ONLY the provided context."},
            {"role": "user", "content": f"Context:\n{context}\n\nQuestion: {question}"}
        ]
    )
    return resp.choices[0].message.content

# Example
print(rag_query("Why is Python bad for CPU-intensive parallel tasks?"))
```

---

## 各组件的关键参数 / Key Parameters per Component

**Embedding 选型**
| 模型 | 维度 | 成本 | 适用 |
|------|------|------|------|
| text-embedding-3-small | 1536 | 低 | 生产首选 |
| text-embedding-3-large | 3072 | 中 | 高精度需求 |
| nomic-embed (本地) | 768 | 免费 | 私有化部署 |

**Chunk 策略**
- 太小 (<100 tokens)：上下文不足，检索碎片化
- 太大 (>1000 tokens)：噪音多，相关性评分稀释
- **推荐：256-512 tokens + 50 token overlap**

**Top-K 选择**
- k=3~5：平衡相关性和上下文长度
- 配合 reranker 可先取 k=20，再精排到 k=5

---

## 三大常见失败模式 / Top 3 Failure Modes

❌ **Embedding 漂移 (Drift)** — 文档用旧版 embedding 模型存储，查询用新模型 → 语义空间不匹配，检索质量崩溃
→ 解决：embedding 版本化，模型升级时重新 index

❌ **上下文超长 (Context Overflow)** — 塞了太多 chunk，LLM 开始"迷路"，中间信息丢失
→ 解决：限制 context token 数量，用 reranker 精选

❌ **幻觉没有被 RAG 消除** — LLM 忽略 context 用自身知识回答
→ 解决：system prompt 明确 "ONLY use the provided context"，加 citation 要求

---

## Agent 扩展：当 RAG 不够用 / When RAG Isn't Enough → Agents

```python
# RAG 的边界：只能回答"知识库里有的"
# Agent 可以调用工具：查数据库、调 API、执行代码

tools = [
    {
        "type": "function",
        "function": {
            "name": "search_knowledge_base",
            "description": "Search internal docs",
            "parameters": {"type": "object", "properties": {
                "query": {"type": "string"}
            }}
        }
    },
    {
        "type": "function", 
        "function": {
            "name": "run_sql_query",
            "description": "Query production database",
            "parameters": {"type": "object", "properties": {
                "sql": {"type": "string"}
            }}
        }
    }
]
# LLM decides which tool to call based on the question
# This is the ReAct (Reason + Act) pattern
```

---

## 📚 References
- https://platform.openai.com/docs/guides/embeddings — OpenAI Embeddings guide
- https://www.pinecone.io/learn/retrieval-augmented-generation/ — RAG fundamentals
- https://python.langchain.com/docs/concepts/rag/ — LangChain RAG concepts
- https://arxiv.org/abs/2005.11401 — Original RAG paper (Lewis et al., 2020)

## 🧒 ELI5
RAG 就像开卷考试：你不需要把所有知识背在脑子里，考试时可以查书（检索相关内容），然后用书上的内容来回答。Agent 则是还可以在考试中打电话查资料或做实验！

RAG is like an open-book exam: retrieve relevant passages, then answer. Agents add the ability to also make phone calls or run experiments during the exam!
